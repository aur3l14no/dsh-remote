--- a/packages/api/session-controller/src/admission.ts
+++ b/packages/api/session-controller/src/admission.ts
@@ -14,6 +14,8 @@
 export interface ApiSessionAdmission {
   /** Owns directory preparation when installed; rejection never falls back to local mkdir. */
   prepare(request: ApiSessionAdmissionRequest): Promise<void>
+  /** Select a composition only after the Session execution identity is admitted. */
+  preset?(sessionId: SessionId, requested: string | undefined): string | undefined
 }
 
 declare module '@deepseek-ai/cordis' {
--- a/packages/api/session-controller/src/agent.ts
+++ b/packages/api/session-controller/src/agent.ts
@@ -394,10 +394,11 @@
    * @param presetId - requested preset or the configured default when omitted.
    * @returns the resolved preset identity and Agent setup callback.
    */
-  async composeAgent(presetId: string | undefined): Promise<{
+  async composeAgent(presetId: string | undefined, sessionId?: SessionId): Promise<{
     readonly agentPreset?: string
     readonly setup: AgentSetup
   }> {
+    if (sessionId !== undefined) presetId = this.ctx.get('apiSessionAdmission')?.preset?.(sessionId, presetId) ?? presetId
     const presets = this.ctx.get('agentPresets')
     if (presets === undefined) {
       return { setup: (_agentCtx, agent) => { this.installSelection(agent) } }
@@ -452,7 +453,7 @@
       throw new ApiSessionSubagentOwnership(sessionId)
     }
     await this.prepareSession({ operation: 'resume', sessionId, cwd: observation.header.cwd })
-    const composition = await this.composeAgent(this.presetForObservation(observation))
+    const composition = await this.composeAgent(this.presetForObservation(observation), sessionId)
     const published = this.ctx.sessions.get(sessionId)
     const live = this.ctx.agents.get(sessionId)
     if (published !== undefined && hasApiSessionSubagentOwner(this.ctx, published, live)) {
@@ -491,7 +492,7 @@
         const storedPreset = this.presetForObservation(observation)
         this.assertPresetUnchanged(sessionId, presetId, storedPreset)
         await this.prepareSession({ operation: 'resume', sessionId, cwd: observation.header.cwd })
-        const composition = await this.composeAgent(storedPreset)
+        const composition = await this.composeAgent(storedPreset, sessionId)
         return (await this.ctx.agents.resume({
           resumeSessionId: sessionId,
           agentOptions: this.agentOptions(),
@@ -508,7 +509,7 @@
     } catch (error: unknown) {
       throw new Error(`failed to ensure project directory "${cwd}": ${String(error)}`, { cause: error })
     }
-    const composition = await this.composeAgent(presetId)
+    const composition = await this.composeAgent(presetId, sessionId)
     return (await this.ctx.agents.create({
       sessionId,
       agentOptions: this.agentOptions(),
--- a/packages/api/session-controller/src/commands.ts
+++ b/packages/api/session-controller/src/commands.ts
@@ -259,7 +259,7 @@
     await this.agents.prepareSession({ operation: 'fork', sessionId: childId,
       sourceSessionId: source.header.id, cwd: source.header.cwd,
       ...(workspace === undefined ? {} : { workspaceId: workspace.id }) })
-    const composition = await this.agents.composeAgent(this.agents.presetForObservation(source))
+    const composition = await this.agents.composeAgent(this.agents.presetForObservation(source), childId)
     try {
       const { provider, model } = this.ctx.agentDefaultModel.currentSelection()
       await this.ctx.agents.create({
--- a/packages/workspace/workspace/src/index.ts
+++ b/packages/workspace/workspace/src/index.ts
@@ -110,7 +110,7 @@
     },
   }
 
-  constructor(ctx: Context) {
+  constructor(ctx: Context, private readonly config: { bootstrapHistory?: boolean } = {}) {
     super(ctx, 'workspaceRegistry')
   }
 
@@ -127,7 +127,7 @@
     if (!this.state.initialized) {
       const headers = await this.listStoredHeaders()
       await this.replaceHeaderIndex(headers)
-      await this.bootstrap(headers)
+      await this.bootstrap(this.config.bootstrapHistory === false ? [] : headers)
     } else if (this.table.size > 0) {
       await this.replaceHeaderIndex(await this.listStoredHeaders())
     }
