---
name: remote-proof
description: Verify the selected remote workspace with a bundled script.
---
Run `sh scripts/proof.sh` using the script path relative to this skill's resource base. Keep the command working directory in the selected workspace. The script needs only a POSIX shell; no installation is required.
